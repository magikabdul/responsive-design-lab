import { createGlobalStyle } from 'styled-components';

const GlobalStylesAdvancedProject = createGlobalStyle`
  body {
    font-size: 10px;
    overflow-x: hidden !important;
  }
`;

export default GlobalStylesAdvancedProject;

