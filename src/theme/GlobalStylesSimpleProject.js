import { createGlobalStyle } from 'styled-components';

const GlobalStylesSimpleProject = createGlobalStyle`
  body {
    font-family: 'Lato', sans-serif;
    font-size:14px;
    line-height:1.5;
    font-weight:400;
    color:#434343;
    background:#FFF;
  }
`;

export default GlobalStylesSimpleProject
